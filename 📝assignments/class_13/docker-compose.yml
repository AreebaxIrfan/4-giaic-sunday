version: "3.9"

services:
  backend:
    build: ./backend_fastapi
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend_next.js
    ports:
      - "3000:3000"
    depends_on:
      - backend
